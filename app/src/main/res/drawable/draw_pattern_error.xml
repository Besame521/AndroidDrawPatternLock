<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 空心圆 -->
    <item
        android:gravity="center">
        <shape
            android:shape="oval"
            android:useLevel="false">
            <size
                android:height="60dp"
                android:width="60dp"/>
            <stroke
                android:width="1dp"
                android:color="@color/draw_pattern_error" />
        </shape>
    </item>

    <item
        android:gravity="center">
        <shape
            android:shape="oval"
            android:useLevel="false">
            <size
                android:height="20dp"
                android:width="20dp"/>
            <solid android:color="@color/draw_pattern_error" />
        </shape>
    </item>
</layer-list>